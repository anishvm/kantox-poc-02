---
kind: Deployment
apiVersion: apps/v1
metadata:
  name: hello-world
spec:
  replicas: 2
  selector:
    matchLabels:
      app: hello-world
  template:
    metadata:
      labels:
        app: hello-world
    spec:
      containers:
      - name: hello-world
        image: ansih/kantox-poc:main
        image: ansih/kantox-poc:main
        ports:
        - containerPort: 8000
